# prop-crm Engineering TODOs

Generated by /plan-eng-review on 2026-04-16. Priority-ordered.

---

## CRITICAL — Do Before Next Client Demo

- [ ] **Move Supabase credentials to .env**
  - Create `.env` with `VITE_SUPABASE_URL` and `VITE_SUPABASE_ANON_KEY`
  - Update App.jsx lines 12-13 to use `import.meta.env.VITE_SUPABASE_URL` etc.
  - Add `.env` to `.gitignore`

- [ ] **Replace CDN import with npm package**
  - `npm install @supabase/supabase-js`
  - Change App.jsx line 2 from `https://esm.sh/@supabase/supabase-js@2` to `@supabase/supabase-js`

- [ ] **Verify Supabase RLS policies (manual, in dashboard)**
  - Every data table (leads, units, activities, deals, profiles, etc.) needs RLS enabled
  - Policies must enforce role-based access server-side — client-side `can()` is not security
  - Test with a non-admin JWT that RLS actually blocks what it should

- [ ] **Fix login flow auto-super_admin bug**
  - App.jsx ~line 557: if `profiles` row is missing, code auto-creates user as `super_admin`
  - Change to default role `viewer` or throw an explicit error requiring admin provisioning

---

## HIGH — This Week

- [ ] **Delete junk files** (9 files, no production use)
  - `src/App - 2026-04-13T113506.312.jsx`
  - `src/App.jsx.bak`
  - `src/App_working.jsx`
  - `src/create-user.js`
  - `aaapackage.json`
  - `bbbbpackage.json`
  - `main` (file in root)
  - `pwd` (file in root)
  - `v1.6-golden` (file in root)
  - Also commit the 3 already-staged deletions: `src/App123.jsx`, `src/Appa123.jsx`, `src/Appabc.jsx`

- [ ] **Fix direct setTab() calls that bypass navigation history**
  - App.jsx line 5627: replace `setTab(...)` with `navigateToTab(...)`
  - App.jsx line 10561: same
  - App.jsx line 10582: same
  - These break the browser back-button sentinel logic

- [ ] **Fix all 65 Supabase select("*") queries**
  - Each query should name only the columns actually used
  - Example: `.select("id, name, status, phone, assigned_to")` instead of `.select("*")`
  - Biggest wins: leads, activities, units tables (most columns, most frequent queries)

- [ ] **Add useMemo to heavy render computations**
  - 399 array operations in render paths, only 2 useMemo calls in the entire app
  - Priority targets: filtered leads lists, dashboard stat aggregations, sorted tables
  - Pattern: `const filteredLeads = useMemo(() => leads.filter(...), [leads, filterState])`

---

## MEDIUM — Next Sprint

- [ ] **Split src/App.jsx into modules** (10,594 lines → ~200 line shell)
  - Proposed structure:
    ```
    src/
      lib/supabase.js          # client init
      lib/rbac.js              # can() function
      components/              # shared UI (navbar, modals, etc.)
      modules/
        sales/
        leasing/
        inventory/
        contacts/
        settings/
    App.jsx                    # router shell only (~200 lines)
    ```
  - Extract one module at a time, test after each extraction

- [ ] **Install Vitest + write 24 identified unit tests**
  - Test plan saved at: `~/.gstack/projects/mah284-bit-prop-crm/mah28-main-eng-review-test-plan-20260416-130349.md`
  - Covers: auth flows, RBAC enforcement, navigation history, stage gate logic, discount approval
  - `npm install -D vitest @testing-library/react @testing-library/user-event jsdom`

- [ ] **Add Playwright E2E tests for critical paths**
  - Login → role-based dashboard render
  - Lead creation → stage progression → deal close
  - Discount request → approval flow
  - Browser back button doesn't exit app

---

## LOW — Backlog

- [ ] Delete `src/AIBubble.jsx` if unused (verify no imports first)
- [ ] Add TypeScript or JSDoc types to the `can()` RBAC function and Supabase query helpers
- [ ] Add React.memo to pure display components receiving large prop objects
- [ ] Consider React Query or SWR for Supabase data fetching (caching, deduplication, loading states)

---

## Already Fixed (recent commits)

- Browser back button never exits app — sentinel state approach (42a2ca9)
- Sales/Leasing switcher visible on white navbar (29f9dd9)
- React hook error in LeasingModule (5967386)
- Leasing dashboard cards navigate with filters, count sync (14a3c9c)
